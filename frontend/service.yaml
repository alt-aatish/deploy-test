apiVersion: v1
kind: Service
metadata:
  name: dharke-frontend-service
  namespace: frontend
  labels:
    name: dharke-frontend
    version: v1.0
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 80
  selector:
    name: dharke-frontend
    version: v1.0